---
name: pipeline-status-amakeng
description: AMAKENG24072026 footage status and pipeline output locations
metadata: 
  node_type: memory
  type: project
  originSessionId: 5030cfc8-6d29-40e8-9eaf-8a678c994dce
  modified: 2026-08-14T15:09:44.242Z
---

AMAKENG24072026 (360/AMAKENG24072026/): **vehicle-mounted** 360 capture (cam on a moving armored vehicle). Clips 001/003 = driving (usable, moving windows 2–258s/268–366s and 8–586s, pre-cut to raw/amakeng_seg{1,2,3}.mp4); clip 002 = fully static (unusable). Clip 004 is `.insv` only (10 GB) — cannot be stitched on ARM64 Linux, needs Insta360 Studio on the user's x86 machine. Clips 005–008 exist only as `.lrv` low-res proxies. Key pipeline lessons (details in SETUP_NOTES.md): 110° FOV views not 90° cube faces; angular-flow masks used ONLY for 3DGS training alpha, never for SfM features; SfM images must be clean RGB (alpha bakes in junk boundary features); mapper needs relaxed init (init_min_tri_angle 4) and best-of-N model selection + merge; plain sequential SfM scale-drifts badly on driving corridors → use rig-constrained SfM (03b_run_colmap_rig.sh); 3DGS opacity_reset_interval must be ≥2 epochs (15000 for ~5.6k images) or pruning collapses the model, and iters ≈ 15× image count.

**Classical SfM route ABANDONED, project reset 2026-08-06.** Every SfM variant on clips 001/003 produced scale-inconsistent geometry (plain sequential: drift; rig-constrained: scrambled — even with clean SuperPoint matches, correct zero-baseline rig, forced wide-baseline init rejected as "unsuitable"; hloc+GLOMAP: 120× smooth drift, trajectory a scribbled tangle in bird's-eye). Two 100k-iter 3DGS trainings on that geometry = transparent mush. Root lesson: fast vehicle-mounted 360 on this corridor gives classical SfM too little usable parallax — **do not retry classical SfM on such footage; run the consecutive-frame-spacing check + bird's-eye plot before ever training.**

**2026-08-21 afternoon — GPS-anchored RETRAIN running + DTM terrain.** Pilot s05_gps (GPS-time-fitted poses, 19b_align_gps.py; outputs colmap_db/sXX/gps + splat_output/sXX_gps) gave NO PSNR gain (train 15.9/val 16.4 vs 16.5/17.3) — the depth scale is parallax-consistent with GPS poses (35_depth_scale_check.py: ~43 m/unit ≈ kappa 39.6; LingBot translation units ≠ depth units, ratio ~1.8 is normal, not a conflict) so sharpness ceiling = capture. Batch 34_retrain_gps.sh ran s05,s01 (s01_gps val 14.4 vs 15.6 old — also no gain) then USER STOPPED IT 2026-08-21 18:25 (all processes/crontab/session-cron cleaned; resumable via RENDER_DONE markers) pending an elevation CSV from ArcGIS — workflow in docs/arcgis_elevation_export.md (expects raw/dtm_profile.csv [+ dsm_profile.csv]). OPS LESSON (again): `pkill -f <script-name>` kills the calling shell when the pattern appears in its own command line — kill by PID. NEVER tail logs/retrain_gps.log (progress bars) — grep -a "^\[20". DSM/DTM: user supplied ArcGIS Earth SCREENSHOTS (raw/DSM.tif, DTM.tif; RGBA, no geotags, blue GPX overlay, Vantor layers) — 36_dtm_profile.py registers via homography ICP on the blue line and calibrates grey->metres on GPS altitude (corr 0.91): terrain 17.5–39 m, grades to ~12%. 32_unity_placement.py now applies per-section chord tilt + height from it (unity_placement.json terrain-aware, _flat.json fallback). After the retrain finishes, re-run 31→33→32 pointing at colmap_db/sXX/gps/poses_aligned.npz + splat_output/sXX_gps to produce the placement for the new splats (scripts currently read the OLD paths; parametrize).

**2026-08-21 GPX ANCHORING DONE (Unity placement ready).** User supplied Insta360 Studio GPX for clip 004 (raw/VID_20260724_152412_00_004.gpx, 532 fixes @1 Hz, 644 s, parsed to raw/gpx_004.npz; ENU origin = first fix). Video->GPS time offset +2.0 s (frame n at (n-1)/10+2 s); GPS fix gaps coincide with parked video ranges (315-366 s, 378-391 s) -> sync verified. FINDINGS: the hand-traced satellite route does NOT match the GPS track (map is rotated/oblique; GPS start->end is 184 m NNW, trace says ~590 m E; GPS path 3005 m vs traced 2642 m) -> old map-based placements were 100-450 m off and scales 0.45-1.95x wrong; GPS replaces the map as placement authority (user acknowledged they may have mis-traced). LingBot section trajectories fit GPS at 1-5 m rms (s08 14 m: tight loop, internally distorted). Scripts: 31_gpx_anchor.py (map fit + per-section time-based Procrustes + dt search), 33_joint_seams.py (joint GPS+seam least squares, s08 seams loose, s10 skipped), 32_unity_placement.py (Parser-inverse + delta -> ENU; Unity x=E,y=Up,z=N with scale (s,s,-s); writes colmap_db/gpx/section_transforms.json + scripts/unity/unity_placement.json + scripts/unity/PlaceAmakengSections.cs editor placer + birdseye_assembly_enu.png). VERTICAL LAW (measured on PLYs): road stayed 3 m below cameras in the OLD frame (kappa init cloud beat the map-scaled trajectory) -> delta has no z offset; road plane z=0 everywhere. s10 = parked (drop, no geometry); s11 mostly parked (check); s08 flagged. Seams after joint fit: 0.3-3.7 m xy (s07/s08/s09 8-15 m). HIDDEN QUALITY LIMITER FOUND: per-section trajectory scale was 1.3-1.95x wrong vs GPS while the init cloud was metric -> scale conflict during training; a GPS-anchored retrain (19_align fitting GPS by time instead of the map window) is the cheapest next quality step (~1.5-2 h/section).

**2026-08-14 22:41 — SCALE-OUT COMPLETE: 18/18 SECTIONS TRAINED.** Full circuit covered (30 GB, splat_output/sXX/ply/point_cloud_29999.ply per section). Val PSNR: s01 15.6, s02 14.9, s03 17.7, s04 14.8, s05 17.3, s06 16.6, s07 14.6, s08 15.2, s09 18.5, s10 19.3, s11 19.3, s12 22.8, s13 14.2, s14 15.0, s15 16.9, s16 14.7, s17 13.9, s18 15.6 (avg ~16.4; static sections best, dense canopy worst). STATUS.md reads ALL DONE — machine safe to shut down; crontab guardian self-disarmed (FINISHED marker present) but entries still installed — remove with `crontab -l | grep -v ensure_watchdog | crontab -`. REMAINING WORK: (1) Unity assembly — de-normalize each section ply (inverse of gsplat Parser(normalize=True) transform; recompute per section by loading Parser and applying transform inverse) into shared map-metres, import via aras-p as 18 splat objects; (2) optional quality upgrades per section (MCMC strategy, longer schedules) or targeted recapture. Quality level: recognizable-but-smeary at driver height (user-acknowledged).: 15/18 sections trained (val PSNR 13.9–19.3, avg ~16.3; static sections s10/s11 best at ~19; dense-canopy s13/s17 worst at ~14). In flight tonight: s18, then autonomous s02/s07 rebuild sweep via scripts/final_sweep.sh (detached; maintains **/home/ldrgx10/360_to_3D/STATUS.md** — THE ground truth for current phase & per-section table; check it on any session start). Machine shuts down ~09:00 2026-08-15; ALL state is shutdown-safe (RENDER_DONE markers, 10k-step training ckpts, resume-aware runner). On power-up the USER CRONTAB (@reboot + */10 ensure_watchdog.sh) auto-resumes unfinished sections — self-disarms once scaleout.log contains FINISHED; remove when done: `crontab -l | grep -v ensure_watchdog | crontab -`. Section failure fixes baked into 18/19 scripts: kappa fallback, adaptive conf, sibling/degenerate-mask borrowing (threshold 0.20), min-cloud assert. DEFERRED BY USER: **Unity assembly** — per-section plys (splat_output/sXX/ply/point_cloud_29999.ply) are in per-section NORMALIZED frames; must apply inverse of gsplat Parser(normalize=True) transform per section to land in shared map-metres, then import as multiple splat objects (aras-p). Fly-throughs: splat_output/sXX/flythrough_*.mp4 (honest driver-height preview; quality = recognizable-but-smeary; user has seen and accepted for now; recapture option remains the quality unlock).

**2026-08-13 SCALE-OUT RUNNING (overnight)**: scripts/30_scaleout.sh batch over 18 sections (resumable via splat_output/sXX/RENDER_DONE markers; per-section: 18_make_section → 19_align → gsplat 30k → 20_render_section). Completed so far: s01 (val 15.6), s03 (17.7), s04 (14.8); s02 failed (empty cloud from NaN kappa in parked stretch — 19_align now has kappa fallback + min-cloud assert; s02 cleared, retry by re-running 30_scaleout.sh after batch finishes). Sharpening BA iteration was NEUTRAL (val 16.1 vs 16.3) — pose accuracy no longer binding; skip BA in scale-out. USER EXPECTATION CHECK: fly-through at driver height (splat_output/sXX/flythrough_*.mp4) reads impressionistic (smear/floaters/hull voids) — below the drivable-circuit bar; decision pending after batch: measure headroom (gsplat MCMC strategy + longer schedule) vs recapture (slow/multi-pass, stabilization off) vs hybrid. PLYs are in per-section NORMALIZED frames — Unity assembly must apply inverse Parser normalization to land all in map metres. NIGHT SAFEGUARDS: watchdog_scaleout.sh (5-min heartbeat logs/watchdog.heartbeat, relaunches stale batch ≤8×, exits on FINISHED) + USER CRONTAB entries (*/10 + @reboot → ensure_watchdog.sh, flock-guarded, survives session death AND reboot — REMOVE crontab entries when project phase done: crontab -l | grep -v ensure_watchdog | crontab -).

**2026-08-12/13 PILOT BREAKTHROUGH (sectioned 10fps architecture)**: pilot section (10fps frames 1300–1800, barrier junction, ~180 m) trained to **masked PSNR train 17.3 / val 16.3** — first recognizable held-out renders of the project (layout, colors, red-white barrier visible; residual smear from ~0.2–0.5 m pose jitter). Pipeline: 18_make_section.sh → 19_align_section.py (rigid Procrustes to route window; NO DTW needed for short sections) → gsplat train (reset-every ≈2.5 epochs, grow-grad2d 1e-4, pose-opt 1e-4, bilateral grid) → 20_render_pilot.py. CRITICAL FIXES found via pilot: (1) **SVD basis handedness** — enforce det(+1) on [b1,b2,n] or all poses are REFLECTIONS (data-dependent; hit pilot, spared full track by luck); check `det` on every SVD-derived basis forever; (2) **gsplat's internal eval PSNR is misleading on masked scenes** (GT hull unmasked → ~7 dB reported while true masked PSNR was 16+; always measure masked); (3) LingBot keyframe-parity jitter hypothesis refuted (even/odd identical); jitter is uniform 0.2–0.5 m. NEXT: decide scale-out to ~18 sections (route fractions per section, then batch 18→19→train, assemble in Unity as multiple splats in shared map frame) vs first reducing smear via windowed world-masked BA (small-error regime now). 10fps derotated frames: frames/amanorth10v/c04 (6,474). Ops law reaffirmed: liveness checks must never embed match-tokens in wrapper cmdlines (bit us ~6 ways); prefer file-marker conditions.

**2026-08-11 findings (quality debugging)**: baseline splat blur diagnosed via sequence of controlled tests. (1) Photometric pose-opt refuted: gsplat pose_opt lr 1e-5 = frozen poses, 1e-3 = ~60 m random-walk divergence — convergence basin ≪ pose error, never retry. (2) COLMAP pose_prior_mapper fragments (29 sub-models); pycolmap create_pose_prior_bundle_adjuster's internal alignment shifted the model ~1 km — prior machinery treacherous, avoid. (3) KEY ASSETS from that detour: hloc windowed matching (scripts/13_hloc_windowed.py, 62k pairs, no same-frame) + triangulation against snapped poses = colmap_db/amanorthv/ba/triangulated, mean reproj 1.63 px → snapped poses ARE consistent at feature level. (4) Init-swap A/B (LingBot cloud vs triangulated points): identical ~11 dB at 1.3 epochs → init was never the bottleneck. (5) Train PSNR ≈ val PSNR ≈ 12 → UNDERFITTING, and GT frames are sharp (no motion blur). Root: gsplat defaults starve densification at this scene scale + far too few epochs. Final run in flight: scripts/run_gsplat_final.sh (80k steps ≈ 15 epochs, grow-grad2d 1e-4, refine-stop 40k, bilateral grid) supervised by scripts/supervise_final.sh → splat_output/amanorthv_final. Ops trap (bitten 3×): any pgrep/grep-based liveness check MUST require two tokens that only the real process has together (e.g. *simple_trainer.py*amanorthv_final*) — launcher wrapper cmdlines linger and contain single tokens.

**NEW ROUTE — WORKING (2026-08-06)**: input = stitched clip 004 ("3A_AMA North 360_.mp4", 8K 647s) + satellite map with traced route ("3A_Ama North Map.png"). Scene `amanorthv` (v = vehicle-locked). CRITICAL: Insta360 exports are FlowState direction-stabilized — must derotate to vehicle-lock first (11_derotate.py) or every downstream assumption breaks. Full pipeline + lessons in SETUP_NOTES "LingBot-Map pipeline"; chain runner: scripts/run_amanorthv_chain.sh, training: scripts/supervise_amanorthv.sh (80k iters, 5180 views). LingBot-Map works on ARM64 (~3 it/s); its rotation/translation heads drift independently — corrections handled separately in 09_snap_to_route.py. Design doc: docs/superpowers/specs/2026-08-06-lingbot-map-splat-design.md. Map scale assumed 1.5 m/px (route 2640 m). Deliverable: Unity via aras-p UnityGaussianSplatting. **Housekeeping 2026-08-06**: deleted colmap_db, splat_output, cubefaces*, frames, raw clips, venv2 (hloc env), 360/AMAKENG24072026 (user has backups). Kept: tools/ (patched gaussian-splatting + COLMAP), venv, scripts, logs, 360/Hunter + 360/Leopard2SG (unrelated captures, NOT ours to delete). Keep-fixes still in tools/gaussian-splatting: uint8 camera storage; and scripts/supervise_amakeng_hloc.sh pattern (checkpointed training supervisor). Loss is uninformative when alpha-masked (hull floor ~0.3). NOTE: root-owned vLLM/uvicorn stack at boot (~27 GB, not killable without sudo).

Pipeline: `scripts/run_pipeline.sh <scene> [--fps N --faces F,R,B,L --matcher colmap|hloc --iters N] <videos...>` → `splat_output/<scene>/point_cloud/iteration_N/point_cloud.ply`. Deliverable target: Unity via aras-p UnityGaussianSplatting.

See [[dgx-spark-toolchain]] for build environment.

- 2026-08-31: real DTM/DSM from QGIS (raw/dtm_profile.csv, dsm_profile.csv) via scripts/37_dtm_csv.py → colmap_db/gpx/dtm_profile.npz; screenshot salvage was 1.8 m rms off. 32_unity_placement.py must run with `venv/bin/python` (system python3 lacks pycolmap) + LD_LIBRARY_PATH colmapdeps/lib. Retrain still stopped, user to decide on resume.
- 2026-08-31: retrain skipped (user). Unity bundle in unity_bundle/ (merged Unity-frame PLY 1.19 GB, centreline CSV, road collider C#, README). Baking scripts 38/39. aras-p: no handedness conversion, near clip culls whole splats by centre depth (verified from shader).
- 2026-08-31 mask audit: training masks (make_masks.py optical flow) blank the ROAD ahead/behind + sky patches (42–75 %/face) — likely a big part of the PSNR-16 ceiling. SegFormer-b2 ADE20K cached for sky masks. Retrain ≈ 2.3 h/section (40 h total); pilot one section first.
- OPS LAW (bitten twice): never `kill $(pgrep -f PATTERN)` or `pkill -f PATTERN` from the Bash tool when PATTERN appears in the command itself — it matches the tool's own `bash -c` shell and kills it (exit 144) mid-script. Get PIDs from a prior `ps -eo pid,cmd | grep X | grep -v "bash -c"` call and kill explicit numbers.
- 2026-08-31 night: v4 batch (masks v2 + near 4 m + sky-alpha loss + analytic road-depth loss, 4500 steps/section) running via scripts/45_batch_v3.sh with TAG=v4; watchdog_v4 + crontab ensure_watchdog_v4; bakes unity_bundle_v4/ at the end.
- 2026-09-01: v4 batch complete (16/16, 0 fail, ~31 min/section at 4500 steps). Deliverable = unity_bundle_v4/ (identity transforms, near clip 4 m). Old unity_bundle/ obsolete. Remaining gap: placement/centreline from SfM poses vs pose-opt shifts of 3-7 m — use checkpoint pose_adjust (train-split indices i%8!=0, c2w @ [rot6d(d[3:]+identity) | d[:3]]).
